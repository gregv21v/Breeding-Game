package main

class Player {
  var position = new PVector(0, 0, 0)
  val speed = 10
  
  
  
  
  
  def moveLeft {
    position.x -= speed
  }
  
  def moveRight {
    position.x -= speed
    
  }
  
  
  def moveUp {
    position.y += speed
  }
  
  def moveDown {
    position.y += speed
  }
}