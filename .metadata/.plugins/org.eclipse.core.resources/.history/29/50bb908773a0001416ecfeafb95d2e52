package main

import scala.collection.mutable.Map

class DNARules {
  
  var rules = Array.ofDim[String](10) // 0 - 9
  var pairs = Array.ofDim[String](16)
  var bases = "ATCG"
  
  
  def generatePairs {
    var i = 0
    for(i <- 0 until bases.length) {
      for(j <- 0 until bases.length) {
        pairs(i) = bases(i) + bases(j)
      }
    }
  }
  
  
  def generateRules {
    
  }

}